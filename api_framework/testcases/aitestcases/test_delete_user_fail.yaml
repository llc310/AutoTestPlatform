-
  feature: 登录模块
  story: 登录接口
  title: 登录成功
  description: 登录admin账号
  requests:
    method: post
    url: /api/private/v1/login
    headers:
      Content-Type: application/json
    json:
      username: admin
      password: '123456'
  extract:
    -
      key: token
      attr: json
      expr: $.data.token
-
  feature: 用户管理模块
  story: 删除用户接口
  title: $parametrize{title}
  description: 删除用户失败-$parametrize{desc}
  requests:
    method: delete
    url: /api/private/v1/users/$parametrize{id}
    headers:
      Authorization: $parametrize{auth}
      Content-Type: application/json
  parametrize:
    title:
      - test_delete_user_fail_01
      - test_delete_user_fail_02
      - test_delete_user_fail_03
      - test_delete_user_fail_04
    desc:
      - id为空
      - id非数字
      - 删除admin账户
      - token无效
    id:
      - ''
      - abc
      - 500
      - 1
    auth:
      - ${read_extract(token)}
      - ${read_extract(token)}
      - ${read_extract(token)}
      - invalidtoken
    msg:
      - 用户ID不能为空
      - ID必须是数字
      - 不允许删除admin账户
      - 无效token
  validate:
    equals:
      -
        - 200
        - status_code
      -
        - 400
        - json
        - $.meta.status
    contains:
      -
        - $parametrize{msg}
        - json
        - $.meta.msg
